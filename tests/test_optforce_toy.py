"""End-to-end on a tiny network whose FORCE set is known by hand.

This is the regression anchor: if this passes, the bilevel reduction is sound.
"""
import pytest


@pytest.mark.skip(reason="build toy network + implement stages 1-4 first")
def test_force_set_toy():
    ...
